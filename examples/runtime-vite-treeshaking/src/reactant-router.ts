import { createBrowserHistory } from 'reactant-router';

console.log('reactant-router tree-shaking probe', createBrowserHistory);
console.log(`typeof createBrowserHistory: ${typeof createBrowserHistory}`);
