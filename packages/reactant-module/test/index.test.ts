// import { createModule, action } from '../src';

// test('base module', () => {
//   interface State {
//     f: string[];
//   }

//   class Foo {
//     state: State = {
//       f: [],
//     };

//     bar = 1;

//     @action
//     change(id: number) {
//       this.bar += id;
//       this.state.f[this.bar] = 'default';
//     }
//   }

//   const foo = createModule(Foo);

//   foo.change(1);

//   expect(foo.state.f[2]).toEqual(['default']);
// });

test('', () => {});
