import { redux } from '..';

test('', () => {
  // redux(({
  //   reducers: {
  //     todoList: (state = [], action) => action.type === 'add' ? [todo, ...state] : state;
  //   },
  //   actions: {
  //     add: (todo) => dispatch => dispatch({ type: 'add', todo }),
  //   }
  // });

});
